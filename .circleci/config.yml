version: 2

jobs:
  build:
    docker:
      - image: circleci/python:latest
    steps: 
      - run: echo "build"  
  deploy:
    docker:
      - image: circleci/python:latest
    steps: 
      - run: echo "deploy"

workflows:
  version: 2
    build_and_deploy:
      jobs:
        - build
        - deploy
 