version: 2
jobs:
  build:
    docker:
      - image: circleci/python:latest
    steps: 
      - run: echo "build"  
  deploy:
    docker:
      - image: circleci/python:latest
    steps: 
      - run: echo "deploy"

workflows:
  build-test-and-deploy:
    jobs:
      - build
      - test1
         